node_modules
dist
.env.local
.env
*.local
.DS_Store
supabase/.temp
*.tsbuildinfo

# Real schedule screenshots used for local extraction testing. These contain
# real instructor names and room numbers — never commit them.
*.PNG
*.png
!public/**/*.png

# Visual brainstorming companion (local only)
.superpowers/
